exclude_patterns:
  - "**/test/"
  - "**/androidTest/"
  - "**test"
version: "2"
checks:
  method-lines:
    config:
      threshold: 35
  similar-code:
    enabled: false
  identical-code:
    enabled: false
  file-lines:
    config:
      threshold: 500